plugins {
  id "org.jetbrains.kotlin.jvm"
  id "io.mehow.laboratory"
}

laboratory {
  feature("Feature") {
    withDefaultOption("Option") { parent ->
      parent.feature("Feature") {
        it.withDefaultOption("Option")
      }
    }
  }
}
