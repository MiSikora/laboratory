plugins {
  id "org.jetbrains.kotlin.jvm"
  id "io.mehow.laboratory"
}

laboratory {
  feature("Feature") {
    packageName = "io.mehow.explicit"

    withFallbackValue("First")
    withValue("Second")
  }

  feature("Feature") {
    packageName = "io.mehow.explicit.switch"

    withFallbackValue("First")
    withValue("Second")
  }
}
