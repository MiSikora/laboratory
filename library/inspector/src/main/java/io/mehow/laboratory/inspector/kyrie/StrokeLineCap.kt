package io.mehow.laboratory.inspector.kyrie

/** Stroke line cap determines the shape that should be used at the corners of stroked paths. */
internal enum class StrokeLineCap {
    /** A butt stroke line cap. */
    BUTT,
    /** A round stroke line cap. */
    ROUND,
    /** A square stroke line cap. */
    SQUARE
}
